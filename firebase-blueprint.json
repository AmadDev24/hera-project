{
  "entities": {
    "Conversation": {
      "title": "Conversation Session",
      "description": "Represents a tax-consultation chatbot conversation session with message history and citations.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique session ID generated by the client or server."
        },
        "userId": {
          "type": "string",
          "description": "Owner user UID or 'anonymous' for guest session."
        },
        "title": {
          "type": "string",
          "description": "Summary title of the conversation."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Initial creation timestamp."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Last updated timestamp."
        },
        "messages": {
          "type": "array",
          "description": "Sequence of talk parts between user and AI Model."
        }
      },
      "required": ["id", "userId", "title", "createdAt", "updatedAt", "messages"]
    },
    "SearchAnalytics": {
      "title": "Search Query Analytics",
      "description": "Log record tracking single search trigger for administrative monitoring.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique analytics ID."
        },
        "query": {
          "type": "string",
          "description": "The search query prompt sent by the user."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when query was triggerred."
        },
        "domainCount": {
          "type": "integer",
          "description": "Number of cited source domains."
        },
        "citedDomains": {
          "type": "array",
          "description": "List of domains referenced during search."
        },
        "clientEmail": {
          "type": "string",
          "description": "Anonymous or logged in client email identifier."
        },
        "feedback": {
          "type": "string",
          "description": "Optional helpful rating or category."
        }
      },
      "required": ["id", "query", "timestamp", "domainCount", "citedDomains", "clientEmail"]
    },
    "FAQ": {
      "title": "Frequently Asked Question",
      "description": "A tax question preset configured by the administrator.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique FAQ identifier."
        },
        "category": {
          "type": "string",
          "description": "Category group of the FAQ."
        },
        "title": {
          "type": "string",
          "description": "Brief title."
        },
        "query": {
          "type": "string",
          "description": "Full consultation query string sent to chatbot."
        },
        "description": {
          "type": "string",
          "description": "Explaining subtext."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Creation timestamp."
        }
      },
      "required": ["id", "category", "title", "query", "description"]
    }
  },
  "firestore": {
    "conversations/{conversationId}": {
      "schema": "Conversation",
      "description": "Primary storage for tax advisor conversational flows."
    },
    "analytics/{analyticsId}": {
      "schema": "SearchAnalytics",
      "description": "Aggregated analytics for admin audit and monitor."
    },
    "faqs/{faqId}": {
      "schema": "FAQ",
      "description": "Preset tax scenario options editable by admin."
    }
  }
}
